export interface FooterSocialLink {
  icon: string;
  link: string;
}

export interface FooterLink {
  label: string;
  url: string;
}

export interface FooterContactInfo {
  icon: string;
  text: string;
  link: string;
}
